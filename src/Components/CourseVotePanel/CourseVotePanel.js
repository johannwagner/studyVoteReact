import * as React from "react";
import {connect} from "react-redux";

class CourseVotePanel extends React.Component{

}

const mapDispatchToProps = null;

const mapStateToProps = state => {
    return {

    }
};

export default connect(mapStateToProps, mapDispatchToProps)(CourseVotePanel)